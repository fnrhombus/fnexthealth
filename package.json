{
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Workspace root for fnexthealth",
  "scripts": {
    "build": "pnpm --filter fnexthealth build",
    "test": "pnpm --filter fnexthealth-tests test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
